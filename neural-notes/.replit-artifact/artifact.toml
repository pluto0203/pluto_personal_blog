kind = "web"
previewPath = "/"
title = "Pluto AI — AI Tech Blog"
version = "1.0.0"
id = "artifacts/neural-notes"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20279

[services.development]
run = "pnpm --filter @workspace/neural-notes run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/neural-notes", "run", "build" ]
publicDir = "artifacts/neural-notes/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20279"
BASE_PATH = "/"
